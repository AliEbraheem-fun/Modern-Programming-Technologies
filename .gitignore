# IDE
.vscode/
.idea/
*.iml

# OS
.DS_Store
Thumbs.db

# Build artifacts
target/
build/
*.class

# Exam intermediate files (contain cleartext answers — never commit)
exam/pool.json
exam/pool.enc.txt
exam/__pycache__/

# Exam password — stored locally only, never committed
exam/.password

# Teacher-only files — never commit
exam/README.md
exam/scrub_callback.py
exam/replacements.txt

# Local snapshots/archives of the repo — may bundle teacher-only exam files
*.zip
